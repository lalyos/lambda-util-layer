"use strict";
exports.__esModule = true;
var Utils = /** @class */ (function () {
    function Utils(Utils) {
        if (!Utils) {
            throw new TypeError("Invalid argument; `value` has no value.");
        }
        this.value = Utils.EMPTY;
        if (Utils && Utils.isUtils(Utils)) {
            this.value = Utils;
        }
    }
    Utils.create = function () {
        return 999;
    };
    return Utils;
}());
exports.Utils = Utils;
